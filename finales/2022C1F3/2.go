package ejercicio

/*La función de Heavside, o función escalón, es una función que vale 0 para valores negativos, y 1 para valores positivos.
Una función escalón corrida es una que tiene este mismo comportamiento, pero donde el salto no necesariamente se
produce en 0. Implementar una función posicion_escalon(funcion, inicio, fin) que reciba una función escalón,
un punto de inicio (donde sabemos que funcion(inicio) = 0) y otro de fin (con funcion(fin) = 1), que determine
en qué valor se produce el salto. Por simplicidad, el salto se producirá si o si en un valor entero, y la función de por sí
recibe números enteros. La función a implementar debe ejecutar de la forma más eficiente posible. Indicar y justificar la
complejidad de la función implementada.*/

func posicionEscalon(funcion func(int) int, inicio, fin int) int {
	// TODO: terminar
	return 0
}